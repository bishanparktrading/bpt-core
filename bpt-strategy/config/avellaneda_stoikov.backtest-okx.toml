# Instance config — BACKTEST mode, AvellanedaStoikov on OKX BTC-USDT-SWAP.
# Reads a canon file produced by bpt-canon-merge over OKX historical
# trades + L2 archives (https://www.okx.com/en-sg/historical-data).

profile_config = "../deploy/config/profile/backtest-okx.toml"

backtest_mode  = true
strategy_config = "strategies/avellaneda_stoikov.backtest-okx.toml"

aeron_config = "../deploy/config/aeron/streams.toml"

[aeron]
media_driver_dir = "/dev/shm/aeron-bpt"

[strategy]
correlation_id = 1006

[metrics]
port = 9104
