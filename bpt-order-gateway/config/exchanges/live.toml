# Exchange adapter endpoints — LIVE trading
# Source of truth for live exchange connectivity.
# Edit this file when adding a new exchange or changing an endpoint.
# Credentials are delivered via systemd-creds at service start, keyed by secret_name.
# Dev fallback: reads ~/.bpt-secrets/<secret_name> (KEY=value format) when CREDENTIALS_DIRECTORY is unset.

[[adapters]]
exchange    = "BINANCE"
secret_name = "bpt/prod/BINANCE"
testnet     = false
rest_host   = "api.binance.com"
rest_port   = "443"
ws_host     = "stream.binance.com"
ws_port     = "9443"
ws_path     = "/ws"
use_tls     = true

[[adapters]]
exchange    = "OKX"
secret_name = "bpt/prod/OKX"
testnet     = false
rest_host   = "www.okx.com"
rest_port   = "443"
ws_host     = "ws.okx.com"
ws_port     = "8443"
ws_path     = "/ws/v5/private"
use_tls     = true

[[adapters]]
exchange    = "HYPERLIQUID"
secret_name = "bpt/prod/HYPERLIQUID"
testnet     = false
rest_host   = "api.hyperliquid.xyz"
rest_port   = "443"
ws_host     = "api.hyperliquid.xyz"
ws_port     = "443"
ws_path     = "/ws"
use_tls     = true
